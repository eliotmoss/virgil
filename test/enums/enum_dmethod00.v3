//@execute 0=42; 1=42
// Dotted def method, static and virtual dispatch
enum E { A, B }
def E.m() -> int { return 42; }
def main(i: int) -> int {
    match (i) {
        0 => return E.A.m();
        1 => return E.B.m();
    }
    return -1;
}
